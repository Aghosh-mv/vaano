name: vaano
description: vaáno – Photo & Video Editor
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  provider: ^6.1.1
  file_picker: ^8.0.0
  image: ^4.1.4
  http: ^1.2.0
  video_player: ^2.8.1
  audioplayers: ^6.0.0
  path: ^1.9.0
  mime: ^1.0.0
  supabase_flutter: ^2.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
